// useful string properties and methods

let userName = "Rosa";
let phoneNumber = "123-456-7890";

// console.log(userName.length); // number of characters in string
// console.log(userName.charAt(0)); // first character
// console.log(userName.indexOf("a")); // index of first instance of "a"
// console.log(userName.lastIndexOf("s")); // index of last instance of "s"
// userName = userName.trim(); // removes whitespace from beginning and end of string
// userName = userName.toLocaleUpperCase(); // changes all characters to uppercase
// userName = userName.toLocaleLowerCase(); // changes all characters to lowercase


phoneNumber = phoneNumber.replaceAll("-", ""); // replaces all instances of "-" with ""
console.log(phoneNumber);